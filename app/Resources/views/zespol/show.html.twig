{% extends 'base.html.twig' %}

{% block body %}
    <h1>Zespol</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ zespol.id }}</td>
            </tr>
            <tr>
                <th>Nazwa</th>
                <td>{{ zespol.nazwa }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('zespol_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('zespol_edit', { 'id': zespol.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
